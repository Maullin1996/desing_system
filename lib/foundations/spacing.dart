

import '../design_system.dart';

/// A class that consolidates foundation spacing definitions for the app.
class DesignSpacingFoundation {
  DesignSpacingFoundation._(); // Private constructor to prevent instantiation.

  // Widget spacing
  /// The spacing between cards (refer to [DesignSpacing] for details).
  static const double spaceBetweenCards = DesignSpacing.spaceXS;
}